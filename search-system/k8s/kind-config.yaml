kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: search-system
nodes:
  - role: control-plane
    # Map host port 8080 → NodePort 30080 so the API is reachable at localhost:8080
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
